---
globs: "src/domain/models/*"
---
# Data Model README Auto-update

**Trigger:** When domain models are modified, compare and create or replace the README.md "Data Model" section to be synced with latest values

**Data Source:** Extract data from entity files containing `@Entity()` decorated classes, properties, and relationships.

---

## 📊 Generated Content

### 1. Mermaid ERD Diagram
**Purpose:** Visual representation of entity relationships and cardinalities

#### Diagram Specifications
- ✅ Include all entities as simple boxes (entity names only)
- ✅ Show relationships with appropriate cardinality symbols
- ✅ Ensure mermaid syntax is valid

#### Diagram example
```markdown
### Entity Relationship Diagram
```mermaid
erDiagram
    User
    Profile
    Feature
    AnotherEntity
    
    User ||--o| Profile : "has one"
    User ||--o{ Feature : "has many"
```

### 2. Entity Descriptions Table
**Purpose:** Display a table with entity, description and key properties columns

### 3. Relationships Table  
**Purpose:** Display a table with relationship descriptions and cardinalities